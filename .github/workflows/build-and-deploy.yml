name: Build & Deploy

on:
  workflow_call: {}

env:
  BUILD_TARGET: Standalon
  LA: ${{ secrets.LALALA}}
  S: ${{ vars.SECRE }}
  env: ${{secrets.ENV_PART}}

jobs:
    buildAndDeploy:
      name: Build & deploy
      environment: ${{needs.extract-env.outputs.env_secrets}}
      runs-on: ubuntu-latest
      steps:  
        - name: Echo secre
          run: echo "$SECRE"
        - name: Echo secre2
          run: echo "${{secrets.SECRE}}"
        - name: Echo lala
          run: echo "$LALALA"
        - name: Echo lala2
          run: echo "${{secrets.LALALA}}"
        - name: Echo2
          run: echo "$BUILD_TARGET"
        - name: Echhooo
          run: echo "${{needs.extract-env.outputs.env_secrets}}"
        - name: Echhooo222
          run: echo "$SEC"
          env:
            SEC: ${{needs.extract-env-outputs.env_secrets}}
        - name: aaa
          run: echo "$env"

